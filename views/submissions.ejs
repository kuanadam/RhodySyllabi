<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/style.css">
    <title>All Submissions</title>
</head>
<body>
    <h1>All Syllabus Submissions</h1>
    <a href="homepage.html"><button>Back to Home</button></a>
    <% if (submissions.length > 0) { %>
        <table>
            <tr>
                <th>ID</th>
                <th>Course</th>
                <th>Professor</th>
                <th>Syllabus File</th>
                <th>Date</th>
                <th>Download</th>
            </tr>
            <% submissions.forEach(submission => { %>
                <tr>
                    <td><%= submission.entry_id %></td>
                    <td><%= submission.course %></td>
                    <td><%= submission.professor %></td>
                    <td><%= submission.syllabi_File %></td>
                    <td><%= submission.syllabi_Date %></td>
                    <td><a href="/uploads/<%= submission.syllabi_File %>" download>Download</a></td>
                </tr>
            <% }) %>
        </table>
    <% } else { %>
        <p>No submissions found.</p>
    <% } %>
</body>
</html>
