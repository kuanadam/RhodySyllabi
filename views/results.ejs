<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/style.css">
    <title>Search Results</title>
</head>
<body>
    <h1>Search Results for "<%= query %>"</h1>
    <a href="homepage.html"><button>Back to Search</button></a>
    <% if (results.length > 0) { %>
        <table>
            <tr>
                <th>ID</th>
                <th>Course</th>
                <th>Professor</th>
                <th>Syllabus File</th>
                <th>Date</th>
            </tr>
            <% results.forEach(row => { %>
                <tr>
                    <td><%= row.id %></td>
                    <td><%= row.course %></td>
                    <td><%= row.professor %></td>
                    <td><%= row.syllabi_File %></td>
                    <td><%= row.syllabi_Date %></td>
                </tr>
            <% }) %>
        </table>
    <% } else { %>
        <p>No results found.</p>
    <% } %>
</body>
</html>
