<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../assets/reset.css">
    <link rel="stylesheet" href="/assets/style.css">
    <title>Search Results</title>
</head>
<body>
    <header class="header">
        <div class ="logo" >
        <img src="/assets/rhodysyllabi_logo.png">
        <a href="homepage.html">RhodySyllabi</a>
        </div>
        <nav class="navbar">
            <a href="homepage.html">Search</a>
            <a href="formsubmission.html">Form</a>
            <a href="submissions">Library</a>
        </nav>
    </header>
    <div class = "container">
    <h1>Search Results for "<%= query %>"</h1>
    
    <% if (results.length > 0) { %>
        <table>
            <tr>
                <th>Course</th>
                <th>Professor</th>
                <th>Syllabus File</th>
                <th>Date</th>
            </tr>
            <% results.forEach(row => { %>
                <tr>
                    <td><%= row.course %></td>
                    <td><%= row.professor %></td>
                    <td><a href="/uploads/<%= row.syllabi_File %>"><%= row.original_filename %></a></td>
                    <td><%= row.syllabi_Date %></td>
                </tr>
            <% }) %>
        </table>
    <% } else { %>
        <p>No results found.</p>
    <% } %>
    </div>
</body>
</html>
